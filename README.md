Chromebook Chat App — Final
- Includes sessions, avatars, friend codes, groups (10 max), DMs, and 1-on-1 audio calls using WebRTC signaling.
- Run: npm install && npm start
- Open http://localhost:3000 in two windows/devices. Upload avatar, add friend by code, invite to group, or call online friends.
